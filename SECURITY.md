# 安全与配置约定

## 禁止提交

- `99_私密配置_禁止提交` 中除说明文件以外的全部内容；
- 服务器、数据库、SMTP、地图服务、WebRTC/TURN 的真实账号、密码、令牌和授权码；
- 私钥、证书、Android 签名文件、本地数据库、用户上传图片和运行日志；
- 包含真实凭据的部署备份、历史压缩包和截图。

## 推荐做法

1. 仓库只保留 `.env.example`，示例值使用明确占位符。
2. 本地开发将真实配置写入被 `.gitignore` 排除的 `.env` 文件。
3. 服务器通过 systemd `EnvironmentFile`、容器 Secret 或受限权限配置文件注入凭据。
4. 凭据一旦出现在提交、日志、截图或聊天记录中，应立即重新生成，而不只是删除文件。
5. 生产接口启用 HTTPS、最小权限、防暴力登录和审计日志。

## 报告问题

发现安全问题时请不要在公开 Issue 中附带凭据、用户数据或可复现的生产攻击参数，应通过仓库维护者的私密渠道报告。

