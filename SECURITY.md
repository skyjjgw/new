# 安全策略

## 支持范围

当前只维护 `main` 分支最新版本。项目仍是比赛原型，尚未承诺长期安全支持或兼容性修复周期。

## 报告漏洞

请优先使用 GitHub 的私密 Security Advisory 报告漏洞。不要在公开 Issue、讨论、截图或日志中提交凭据、用户图片、邮箱、精确位置、服务器访问方式或可直接利用的生产攻击细节。

报告应包含受影响模块、版本/提交、影响、最小复现步骤和建议缓解措施。维护者确认前不要公开披露。

## 禁止提交

- SMTP 授权码、地图安全密钥、API/设备/TURN Token；
- SSH 私钥、证书私钥、Android keystore 与签名密码；
- 数据库、用户上传、快照、精确轨迹、日志和备份；
- 包含真实凭据或生产 IP 清单的脚本、截图和故障记录；
- 从生产环境导出的原始用户数据。

## 生产基线

1. 关闭 `VISIONBRIDGE_EMAIL_DEBUG` 和演示数据；
2. 为管理接口启用独立认证、最小权限和审计日志；
3. 全站 HTTPS，限制 SSH、TURN、MediaMTX 管理端口的来源；
4. 为登录、上传、审核和媒体授权设置速率限制；
5. 独立生成并定期轮换会话、上传、媒体和 TURN 密钥；
6. 对数据库与图片执行加密备份、恢复演练和保留期清理；
7. 启用 Dependabot、Secret Scanning、push protection 与 CodeQL（仓库权限支持时）。

凭据一旦进入提交历史或聊天记录，先吊销并重新生成，再处理文件和历史；仅删除明文不能解除泄露风险。
