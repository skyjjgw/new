# 贡献指南

## 开发流程

1. 从 `main` 创建短生命周期分支；
2. 不提交 `.env`、数据库、用户上传内容、日志、安装包或服务器备份；
3. 修改接口时同步更新 `docs/` 与 App/大屏调用；
4. 提交前运行受影响模块的测试；
5. 提交信息建议使用 `feat:`、`fix:`、`docs:`、`refactor:`、`test:` 或 `chore:` 前缀。

## 完成标准

- 管理大屏可以构建，相关 API 测试通过；
- Flutter 代码通过 `flutter analyze` 和 `flutter test`；
- 边缘端改动至少通过 Python 语法检查，并说明对应硬件验证结果；
- 新增配置项已加入示例文件，且示例中没有真实凭据；
- 用户可见行为和部署变化已写入 README 或 `docs/`。

